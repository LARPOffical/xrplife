ComranksConfig = {}
ComranksConfig = setmetatable(ComranksConfig, {})

ComranksConfig.Ranks = {
    {rank = "User", perms = {}},
    {rank = "Moderator", perms = {}},
    {rank = "Administrator", perms = {}},
    {rank = "Founder", perms = {}}
}